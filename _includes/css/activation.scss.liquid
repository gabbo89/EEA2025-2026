/* activation.scss.liquid
   Placeholder for Just the Docs nav activation styles.
   The original theme provides SCSS here; if you have the original
   `activation.scss.liquid` contents, replace this file with it.
*/

/* Minimal safe rule to avoid empty <style> content causing linters to complain */
:root { --jtd-placeholder: 1; }
